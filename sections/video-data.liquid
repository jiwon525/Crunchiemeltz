{% comment %}
Video Data Section
This section only stores videos (URL + thumbnail). 
It doesn’t render anything visible by itself.
{% endcomment %}

{% schema %}
{
  "name": "Video Data",
  "settings": [],
  "blocks": [
    {
      "type": "video",
      "name": "Video",
      "settings": [
        { "id": "video_url", "type": "url", "label": "Video URL" },
        { "id": "thumbnail", "type": "image_picker", "label": "Thumbnail" }
      ]
    }
  ],
  "presets": [
    { "name": "Video Data" }
  ]
}
{% endschema %}
